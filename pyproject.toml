[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bestshotai"
version = "0.1.0"
description = "Présélection temporelle locale et embeddings visuels de vidéos."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "BestShotAI contributors" }]
dependencies = [
    "av>=12.0",
    "numpy>=2.0",
    "PyYAML>=6.0",
    "typer>=0.12",
]

[project.scripts]
bestshot = "bestshot.cli:app"
bestshot-gui = "bestshot.desktop.application:main"

[project.optional-dependencies]
embedding = [
    "huggingface-hub>=0.24",
    "Pillow>=10.0",
    "torch>=2.3",
    "torchvision>=0.18",
    "transformers>=4.44",
]
desktop = [
    "PySide6>=6.7",
]
dev = [
    "mypy>=1.10",
    "pytest>=8.0",
    "ruff>=0.5",
]

[tool.hatch.build.targets.wheel]
packages = ["src/bestshot"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra"
markers = [
    "integration: test nécessitant des exécutables ou fichiers réels.",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.mypy]
python_version = "3.12"
strict = true
packages = ["bestshot"]

[[tool.mypy.overrides]]
module = ["huggingface_hub", "torch", "transformers"]
ignore_missing_imports = true
